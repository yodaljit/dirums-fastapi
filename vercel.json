{
    "devCommand": "uvicorn index:app --host 0.0.0.0 --port 8000",
    "builds": [
      {
        "src": "index.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "index.py"
      }
    ]
  }